# Transport Debug Map UI

Интерактивная карта для тестирования и дебага API транспортных станций.

## Как запустить

1. Убедитесь, что в `.env` файле настроен Mapbox токен:
   ```
   MAPBOX_ACCESS_TOKEN=pk.your_token_here
   ```

2. Запустите API сервер:
   ```bash
   go run ./cmd/api/main.go
   ```

3. Откройте в браузере:
   - http://localhost:8080/debug/map
   - или http://localhost:8080/static/transport-map.html

## Функционал

### Поиск станций
- **Клик на карту** - устанавливает точку поиска и отправляет запрос к API
- **Радиус поиска** - визуализируется кругом на карте
- **Типы транспорта** - фильтрация по metro, train, tram, bus, ferry

### Отображение результатов
- **Маркеры на карте** - цветные точки для каждого типа транспорта
- **Sidebar** - список станций с детальной информацией
- **Hover/Click** - popup с информацией о станции и линиях

### Информация о станции
- Название и тип станции
- Линейное расстояние (прямая линия)
- Пешеходное расстояние (примерное)
- Время пешком
- Линии транспорта с цветами

## API Endpoints используемые интерфейсом

### GET /api/v1/debug/config/mapbox
Получение Mapbox токена для инициализации карты.

### GET /api/v1/debug/enrichment/transport
Поиск ближайших станций транспорта.

**Параметры:**
- `lat` - широта (обязательно)
- `lon` - долгота (обязательно)
- `max_distance` - радиус поиска в метрах (по умолчанию 1500)
- `limit` - максимум станций (по умолчанию 10)
- `types` - типы транспорта через запятую

**Пример:**
```
GET /api/v1/debug/enrichment/transport?lat=41.3851&lon=2.1734&max_distance=1500&limit=10&types=metro,train
```

## Цветовая схема типов транспорта

| Тип | Цвет |
|-----|------|
| Metro | 🔴 #ff6b6b |
| Train | 🔵 #4ecdc4 |
| Tram | 🟡 #ffe66d |
| Bus | 🟢 #95e1d3 |
| Ferry | 🟣 #6c5ce7 |

## Keyboard Shortcuts

- Прокрутка колесом мыши - zoom карты
- Drag - перемещение по карте
- Double-click - zoom in

## Troubleshooting

### Карта не загружается
- Проверьте MAPBOX_ACCESS_TOKEN в .env
- Убедитесь что токен валидный на https://account.mapbox.com

### Станции не отображаются
- Проверьте что база данных запущена и содержит данные транспорта
- Проверьте логи сервера на ошибки
- Попробуйте увеличить радиус поиска
